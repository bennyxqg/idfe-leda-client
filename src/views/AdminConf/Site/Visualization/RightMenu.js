import React from 'react';
import { Table, Button, Form, Select, Input, message, Modal } from 'antd';
import { formatTime } from '@/utils/helper'

const Index = () => {
	return (
		<div className="vis-wrap-rightMenu">right</div>
	)
}

export default Index